<Window x:Class="SFTracker.Views.UpdateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Оновлення" Height="160" Width="380"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterOwner">

    <Border Background="{StaticResource SurfaceBrush}"
            CornerRadius="12"
            BorderBrush="#33FFFFFF"
            BorderThickness="1"
            Padding="24">
        <StackPanel VerticalAlignment="Center">
            <TextBlock Text="Встановлення оновлення..." FontSize="15" FontWeight="SemiBold"
                       Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,16"/>
            <ProgressBar x:Name="UpdateProgress" Minimum="0" Maximum="100" Height="6"/>
            <TextBlock x:Name="PercentLabel" Text="0%" FontSize="12"
                       Foreground="{StaticResource TextSecondaryBrush}"
                       HorizontalAlignment="Right" Margin="0,8,0,0"/>
        </StackPanel>
    </Border>
</Window>
